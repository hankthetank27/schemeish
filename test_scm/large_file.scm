(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         
(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)

(define (map ls fn)
  (if (null? ls) 
    () ; cooool!
    (cons (fn (car ls))
          (map (cdr ls) fn))))

(define lx (list 1 2 3 4)) ;comment here!
; we got comment
(define ly (cons 5 (cdr (cdr lx))))
(define mx (map ly (lambda (x) (* x 2)))) ;end w a comment :)
